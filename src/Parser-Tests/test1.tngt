#using if-else Statements

driver<: :>
(:
    var int a := 8;
    if(a%2 = 0)
    (:
        #checking for even no.
        if(i%2 = 0)
        (:
            print(i + ",");
        :) 
    :)
    else
    (:
        #checking for odd no.
        if(i%2 != 0) 
        (:
            print(i + ",");
        :)
    :)
:)
#using functions

void digitCount<: var int n :>
(:
    var int count := 0;
    while(n>0)
    (:
        count := count + 1;
    :)
    print(count + "\n");
:)

driver<: :>
(:
    var int n := 16;
    digitCount(n);
:)
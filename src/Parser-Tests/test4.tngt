#using logical Operators

driver<: :>
(:
    var string word := "malayalam";
    # var int len := word.length();
    var int count := 0;
    for(var int i := 0; i < len; i:= i + 1)
    (:
        print(a);
        print("is divisible by both 2 and 3\n");
    :)

    #using OR
    var int b := 4;
    if(b%2 = 0 | b%3 = 0)
    (:
        print(b);
        print("is divisible by at least one of 2 and 3\n");
    :)

    #using NOT
    var int c := 15;
    if(c%3 != 0)
    (:
        print(c);
        print("is not divisible by 3\n");
    :)
    


:)
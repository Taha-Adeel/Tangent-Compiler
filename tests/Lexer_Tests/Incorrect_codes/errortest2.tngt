# incorrect spelling of driver - will be treated as an identifier
driiver <: :>
(:
    # Use of illegal constants
    var int a := 123_4;
    var float b := .;
    var int c := 789$89;

    # Illegal characters
    ~~ 
    ``
    $$ 

    # Illegal variable names
    var Image $$*, 12$$;
    
:)